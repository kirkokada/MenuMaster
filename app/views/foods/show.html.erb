<% provide :title, @food.name %>

<div class="row">
	<div class="span6 offset3">
		<h1><%= @food.name %></h1>
		<h2>Nutrient Information (per 100g):</h2>
		<div class="stats">
			<span>
				Total Calories:
				<strong class="stat">
					<%= @food.calories %> kcal
				</strong>
			</span>
			<span>
				Carbs:
				<strong class="stat">
					<%= @food.carbs %>g
				</strong>
			</span>
			<span>
				Protein:
				<strong class="stat">
					<%= @food.protein %>g
				</strong>
			</span>
			<span>
				Fat:
				<strong class="stat">
					<%= @food.fat %>g
				</strong>
			</span>
		</div>
	</div>
</div>