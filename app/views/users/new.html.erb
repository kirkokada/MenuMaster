<% provide :title, 'Sign up' %>

<div class="row">
	<div class="span6 offset3">
		<h1>Sign up</h1>
		<%= form_for @user do |f| %>

			<%= render 'fields', f:f %>

			<%= f.submit "Create account", class: "btn btn-primary" %>

		<% end %>
	</div>
</div>
